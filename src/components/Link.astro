---
type Props = astroHTML.JSX.AnchorHTMLAttributes;
const { class: klass, ...props } = Astro.props;
---

<a
  {...props}
  class:list={[
    "text-pink-200 underline hover:no-underline focus-visible:no-underline",
    klass,
  ]}><slot /></a
>
